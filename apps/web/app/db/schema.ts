/**
 * GENERATED FILE — do not edit by hand.
 *
 * Run `pnpm db:introspect` to regenerate this from the migrated database. Schema changes go in
 * `db/migrations/*.sql` (the single source of truth; ADR 0011). A PreToolUse hook blocks edits here.
 *
 * It is intentionally empty until the first `db:introspect` runs against the core ledger migration.
 */
export {};
