# db/reference/llm

Destination for committed raw captures of the **LLM/OCR extraction contract** — cited by
`docs/integrations/llm-extraction.md`. Captures land here via the `regulatory-update` skill (dated
copy of the OpenAI-compatible `/v1/chat/completions` vision request/response shape and the
Qwen2.5-VL model card), so the propose-only extraction layer is grounded in a committed source.

Empty until the first capture is committed.
